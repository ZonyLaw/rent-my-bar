<h1>Rent my bar</h1>

<div class="container">
  <div class="list-index-card-wrapper">

      <%@bars.each do |bar|%>
        <div class="bar-index-card">
        <%=link_to bar_path(bar) do %>
          <%= image_tag 'bar2', class: 'image-list-cover',  alt: "Bar 1 layout" %>
        <%end%>
          <div class="bar-card-content">
            <div>
              <h4><%=bar.name%></h4>
              <p><%=bar.address%></p>
            </div>

            <div>
              <p><span class="bold">£<%=bar.price%></span>/day</p>
            </div>
          </div>
        </div>
      <%end%>
  </div>

</div>
